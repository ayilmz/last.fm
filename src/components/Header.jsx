import React from 'react';
import './header.css';

export const Header = () => (
  <header>
    <div className="wrapper">
      <div>
        <h1>last.fm</h1>
      </div>
    </div>
  </header>
);

Header.propTypes = {
};

Header.defaultProps = {
};
